package frc.robot.commands;

// Empty stub — not implemented yet, and not wired up to any controller binding or
// subsystem. Don't confuse this with JiggleCommand, which is the one actually used
// to shake loose stuck game pieces.
public class WiggleCommand {}
